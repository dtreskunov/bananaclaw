# Live web dictation: host-only credential; never forwarded to agent containers.
ELEVENLABS_API_KEY=
# elevenlabs (default) or disabled. Each group can override this in Agent Settings.
DEFAULT_VOICE_INPUT_BACKEND=elevenlabs
